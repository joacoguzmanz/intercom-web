import React from 'react';
import Layout from '../components/layout';

const ContactPage = () => {
  return (
    <Layout pageTitle='Contacto'>
      <p>Página de contacto en vivo</p>
    </Layout>
  );
};

export const Head = () => <title>Página de contacto</title>;

export default ContactPage;
